#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i < (n); ++i)

int main(){
    int d;
    cin >> d;

    double WaterPressure = (double)d/100;

    cout << WaterPressure << endl;

    return 0;
}